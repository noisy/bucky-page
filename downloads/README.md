# Downloads

Put the Android build here as `bucky-latest.apk` before publishing; the
download page links to it (path and version are in `../site.json`). APK
files are gitignored, so they never land in the repository. The build copies
this folder to `dist/downloads/`.
